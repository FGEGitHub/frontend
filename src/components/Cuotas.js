import { useParams } from "react-router-dom"

const Cuotas =() => {
    let params = useParams()
    console.log(params)
    console.log('etc')

    return (
    <div>

    </div>
    )
}
export default DetalleCliente